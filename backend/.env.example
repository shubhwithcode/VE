PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=vishwakarma_enterprises
DB_AUTO_INIT=true

JWT_SECRET=change_this_secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# If true, /api/auth/login returns { token } (and the backend also accepts Authorization: Bearer <token>).
# Default: true in development, false otherwise.
AUTH_EXPOSE_TOKEN=true
